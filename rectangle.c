#include <stdio.h>
main()
{
    float l,w;
    printf("Enter length of rectangle:");
    scanf("%f",&l);
    printf("Enter width of rectangle:");
    scanf("%f",&w);
    float area;
    area = l * w;
    printf("Area of rectangle is:%f",area);
}
